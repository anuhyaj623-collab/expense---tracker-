# Expense Tracker

A Python-based personal expense tracker that reads CSV data and summarizes spending by category.

## Features
- Loads expense data from CSV
- Groups and totals spending by category
- Displays a clean formatted summary report

## How to run
python analyze.py

## Tech used
- Python (csv, collections)
- CSV data format
- Git + GitHub for version control
